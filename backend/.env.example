MONGO_URI=mongodb://localhost:27017
# Comma-separated list of origins allowed to call this API. Set to your
# frontend's real address: a LAN IP for same-network classroom use
# (http://192.168.1.42:5173), or your deployed frontend's domain for real
# hosting (https://your-app.vercel.app).
CORS_ORIGINS=http://localhost:5173
